# Minimal Template - Basic Module Structure

This template creates a basic Odoo 19+ module with:

- Simple model with basic fields
- List and form views
- Basic security configuration
- Menu structure
- Odoo 19+ compliance (uses `<list>` instead of `<tree>`)
- Mail thread integration